C++二维数组在内存中是按照行优先的方式按照一维顺序关系存放的。因此对于二维数组，可以理解为一维数组的数组，数组名是它的首地址，这个数组的元素个数就是行数，每个元素
都是一个一维数组。例如，声明一个二维int类型数组：

int array2[3][3] = {{11,12,13}, {21,22,23}, (31,32,33)};

array2[0]是一个长度为3的一维数组，当array2[0]在表达式中出现时，表示一个指向该一维数组首地址的整型指针，这和一维数组的数组名表示指向该数组首地址的指针是一样的道理，
所以可用*(array2[0])来表示array2[0][0]；